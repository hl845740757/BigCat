# Protobuf工具

该工具用于将proto文件解析为结构化内存数据

## Protobuf文件规范

1. 字段、枚举和方法必须在单行内完成定义，不可以换行
2. Service、Message、Enum的结束符`}`需要换行
3. 文件名、顶层消息名、服务名不可重复 -- 方便定位
4. 仅支持单行注释`//`，不支持多行注释块`/** */`
5. 注释行与要注释的元素之间不可有空行，空行会中断注释累积，行尾注释有效。